const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open?'true':'false')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();
const form=document.getElementById('contactForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const subject=encodeURIComponent('DTG Enquiry — '+(data.get('organisation')||data.get('name')));const body=encodeURIComponent(`Name: ${data.get('name')}\nOrganisation: ${data.get('organisation')}\nEmail: ${data.get('email')}\n\nProject enquiry:\n${data.get('message')}\n\nWebsite preview only — send manually once the DTG business email is connected.`);const note=document.getElementById('formNote');note.textContent='Enquiry prepared. DTG business email is not connected yet, so nothing was transmitted.';note.style.color='#e0bd7a';console.log('Prepared subject:',subject,'body:',body)})}
